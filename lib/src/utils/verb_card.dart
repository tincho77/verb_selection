import 'package:flutter/material.dart';

Widget verbCard() {
  return Center(
      child: Column(
    mainAxisAlignment: MainAxisAlignment.center,
    children: <Widget>[
      Container(
        child: TextButton(onPressed: () {}, child: Text('Verbos Irregulares')),
      )
    ],
  ));
}
